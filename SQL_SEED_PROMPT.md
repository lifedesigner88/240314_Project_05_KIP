# AI SW Maestro 17th Demo SQL Seed Prompt

아래 프롬프트는 `AI·SW마에스트로 17기` 공식 소개와 모집 공고를 참고해, 현재 KIP 데모 DB를 `면접관 시연용`으로 풍부하게 확장하는 `MariaDB용 idempotent SQL`을 생성하기 위한 문서다.

## 조사 반영 메모

- AI·SW마에스트로는 AI·SW 최고 전문가 집중 멘토링, 팀 프로젝트, 창업 집중형 프로그램 중심의 실전형 인재 양성 사업이다.
- 17기 선발 인원은 `서울센터 300명`, `부산센터 150명` 기준으로 안내된다.
- 17기 과정은 서울센터 기준 `예비 과정 2026-04-07 ~ 2026-05-28`, `본 과정 2026-06-08 ~ 2026-11-20`으로 안내되고, 부산센터는 `예비 과정 2026-05-12 ~ 2026-06-22`, `본 과정 2026-07-01 ~ 2026-12-11`로 안내된다.
- 공식 소개의 프로젝트 톤은 AI 학습, 접근성, 모빌리티, 운영 자동화 등 `AI/SW 기반 문제 해결형 서비스`에 가깝다.
- 이 문서의 더미데이터 컨셉은 위 공식 세계관을 따르되, 면접관이 몇 번의 클릭만으로 구조를 이해할 수 있게 `서울센터 / 부산센터 / 팀 프로젝트 / 멘토링 / 창업 문서` 중심으로 정리한다.

참고 소스:

- [AI·SW마에스트로 공식 홈페이지](https://swmaestro.ai/sw/main/main.do)
- [AI·SW마에스트로 제17기 연수생 모집 공고](https://swmaestro.ai/sw/main/front/notifyMentee.do)

## 사용 목적

- 인프라 레이어에서 `infra-handoff/seed-rich.sql` 같은 파일을 생성하기 위한 프롬프트
- 이미 떠 있는 `MariaDB`에 직접 실행 가능한 SQL 생성
- 현재 앱 시드를 완전히 버리지 않고, `필요한 insert/update`로 17기 운영 데모 세계관에 맞추는 구조

## 프롬프트

```text
당신은 MariaDB 11용 시드 데이터 SQL을 작성하는 시니어 데이터 엔지니어다.

목표:
- 현재 KIP 초기 DB를 `AI SW 마에스트로 17기 운영/멘토링/팀 프로젝트 데모`로 확장하는 idempotent SQL 스크립트를 작성해야 함.
- 결과물은 인프라에서 바로 실행 가능한 순수 SQL이어야 함.
- 이 더미데이터의 1차 독자는 면접관이므로, 몇 번의 조회만으로도 구조와 스토리가 이해되는 형태여야 함.
- 기존 관리자 계정 `소마멘토`는 그대로 로그인 가능해야 함.
- 설명 문장 없이 SQL만 출력해야 함.

중요 제약:
1. 대상 DB는 MariaDB 11이어야 함.
2. 기존 데이터는 유지하되, 세계관 정합성을 위해 필요한 `INSERT`와 `UPDATE`는 허용해야 함.
3. `DELETE`, `TRUNCATE`, `DROP`은 금지됨.
4. 동일 SQL을 여러 번 실행해도 중복 삽입되거나 같은 레코드가 계속 변경되지 않도록 idempotent 하게 작성해야 함.
5. FK 연결 시 하드코딩된 숫자 ID만 믿지 말고, 가능하면 employee_id, group_name, title 같은 business key 기반 서브쿼리를 사용해야 함.
6. `group`는 예약어이므로 테이블명은 반드시 백틱으로 감싸서 `group`으로 사용해야 함.
7. 신규 사용자의 employee_id는 반드시 정규식 `^asm-\\d{4}$`를 만족해야 함.
8. 신규 사용자 비밀번호는 모두 로그인 가능해야 하며, 평문 `1234`에 해당하는 아래 해시를 그대로 사용해야 함.
   - `{bcrypt}$2a$10$wLJ8/V6Vn0no02U8hBtjquLjmk9P4JUxvim6.INyXqVbZE5rgYiwa`
9. 관리자 계정은 반드시 `asm-1234`를 사용해야 함.
10. 신규 일반 사용자는 반드시 `asm-0001`부터 순차 번호 정책을 따라야 함.
11. 문서 데이터는 반드시 `version` 테이블과 함께 들어가야 함.
12. 그룹 문서는 각 그룹마다 최상단 `SECTION` 문서 1개가 필요함.
13. 그룹 문서 체인은 `up_link_id / down_link_id`가 일관되어야 함.
14. 승인 완료된 권한 요청(`request.is_ok = 'Y'`)은 실제 열람 권한을 의미하므로 `user_doc_authority`에도 대응 row를 생성해야 함.
15. 대규모 데이터(수백 명 사용자, 수백 개 팀, 수백 개 문서)는 `WITH RECURSIVE`, 숫자 시퀀스용 CTE, 임시 테이블 등을 활용해 생성해야 함.
16. 날짜는 17기 운영 일정에 맞춰 `2026년` 기준으로 자연스럽게 배치해야 함.
17. 출력 형식은 SQL only여야 하며, 마크다운은 금지됨.

현재 유지해야 할 핵심 계정:

[admin]
- `asm-1234` / `소마멘토` / ADMIN / 비밀번호 `1234`

현재 이미 존재하는 기준 데이터:

[user]
- `asm-1234` / `소마멘토` / ADMIN
- `asm-0001` / `김민서 PM`
- `asm-0002` / `이도윤 Backend`
- `asm-0003` / `박서윤 Frontend`
- `asm-0004` / `정하늘 AI`
- `asm-0005` / `최지훈 QA`
- `asm-0006` / `장유진 Mentor`
- `asm-0007` / `한서준 PO`

[group]
- 루트 그룹: `AI SW 마에스트로`

[document]
- 공개 문서
  - `데모 사용 가이드`
  - `면접 시연 체크리스트`
  - `프로젝트 한 줄 소개`

현재 테이블:
- `user`
- `group`
- `group_user`
- `document`
- `version`
- `request`
- `note`
- `bookmarks`
- `comment`
- `hash_tag`
- `doc_hash_tag`
- `attached_file`
- `user_doc_authority`

테이블 핵심 컬럼 규칙:

[user]
- id
- name
- email
- phone_number
- password
- profile_image_url
- employed_day
- employee_id
- role

[group]
- id
- group_name
- group_type (`DEPARTMENT` or `BUSINESS`)
- super_group_id

[group_user]
- group_id
- user_id
- group_role (`SUPER` or `NORMAL`)

[document]
- id
- uuid
- title
- kms_doc_type (`SECTION` or `CONTENT`)
- book_count
- up_link_id
- down_link_id
- group_id

[version]
- id
- content
- message
- is_show (`Y` or `N`)
- document_id
- writer_id

[request]
- id
- due_date
- is_ok (`P`, `Y`, `N`)
- requester_del_yn
- del_yn
- days
- document_id
- requester_id
- group_id

[note]
- id
- is_read
- message
- writer_id
- receiver_id
- document_id

[bookmarks]
- id
- documentId
- user_id

[comment]
- id
- comment
- user_name
- super_comment_id
- documentId

[hash_tag]
- id
- tag_name

[doc_hash_tag]
- document_id
- hash_tag_id

[attached_file]
- id
- file_name
- file_type
- file_url
- document_id

[user_doc_authority]
- user_id
- document_id
- due_date

조사 기반 더미데이터 세계관:
- `AI SW 마에스트로 17기` 운영 시나리오를 반영해야 함.
- 구조는 `서울센터 300명`, `부산센터 150명` 기준이어야 함.
- 프로그램 톤은 단순 학사 관리가 아니라 `멘토링 + 팀 프로젝트 + 창업 아이템 기획/개발` 중심이어야 함.
- 팀별 프로젝트는 AI, SaaS, 교육, 접근성, 모빌리티, 로컬서비스, 운영자동화, 헬스케어, 생산성, 커뮤니티, 데이터 분석 같은 주제에 맞춰 현실적으로 분산되어야 함.
- 문서 내용은 학생 과제 느낌보다 `실제 예비창업팀이 남길 만한 기획/기술/사업 문서`처럼 작성되어야 함.

이번에 생성/수정할 데이터 요구사항:

1. 조직 구조
- 루트 `AI SW 마에스트로` 아래에 `제17기 운영` 그룹이 있어야 하며, 그 아래에 `서울센터`, `부산센터`가 구성되어야 함.
- `서울센터` 아래에 팀 그룹 `100개`, `부산센터` 아래에 팀 그룹 `50개`가 생성되어야 함.
- 팀 그룹명은 사람이 한눈에 알아볼 수 있게 예를 들어 `서울 17기 001팀`, `부산 17기 001팀` 같은 규칙이어야 함.
- 센터 그룹과 운영 그룹은 `DEPARTMENT`, 각 팀 그룹은 `BUSINESS`로 구성되어야 함.

2. 사용자
- 연수생 사용자 총 `450명`이 생성되어야 함.
  - 서울센터 `300명`
  - 부산센터 `150명`
- 연수생 이름은 현실적인 한국인 이름 위주여야 하며, 중복은 최소화해야 함.
- 멘토 사용자는 충분히 생성되어야 하며, 이름은 반드시 `이름 + 공백 + 멘토` 형식으로 끝나야 함.
- 멘토 수는 각 팀에 멘토 3명을 배치하면서도 멘토 1명당 최대 3팀만 담당하도록 충분히 확보되어야 함.
- `서울센터 운영`, `부산센터 운영`, `프로그램 운영`, `데모데이 운영` 같은 운영진 사용자도 추가해야 함.

3. 팀 배치 규칙
- 각 팀에는 연수생 `정확히 3명`이 속해야 함.
- 연수생 1명은 반드시 `정확히 1개 팀`에만 속해야 함.
- 각 팀에는 멘토 `정확히 3명`이 배정되어야 함.
- 멘토 1명은 서로 다른 팀 `1~3개`까지만 담당해야 함.
- 같은 멘토가 동일 팀에 중복 배정되면 안 됨.
- 팀 그룹의 group_user에는 최소 1명의 대표 멘토가 `SUPER`여야 하며, 나머지는 `NORMAL`이어도 됨.

4. 팀별 창업 아이템과 문서
- 총 150개 팀 모두 고유한 창업 아이템/서비스명을 가져야 함.
- 각 팀 그룹에는 최상단 안내 문서 외에 `최소 5개 이상의 핵심 기획 문서`가 있어야 함.
- 문서 카테고리는 아래와 같은 구성이어야 함.
  - `팀/문제 정의`
  - `시장 조사 및 경쟁 분석`
  - `사용자 페르소나 및 고객 여정`
  - `서비스 기획서`
  - `기술 아키텍처`
  - `AI/데이터 활용 전략`
  - `MVP 개발 계획`
  - `비즈니스 모델 및 수익화`
  - `데모데이 발표 초안`
- 모든 팀이 모든 카테고리를 다 가질 필요는 없지만, 각 팀마다 적어도 5개 카테고리 이상의 문서가 있어야 함.
- 각 팀 문서에는 창업/개발 관련 해시태그가 풍부하게 달려 있어야 함.
- 팀 문서 내용은 너무 짧지 않아야 하며, 실제 팀이 작성한 문서처럼 구체적인 문제, 고객, 기능, 일정, 기술선택, 수익모델이 드러나야 함.

5. 공개 문서
- 공개 문서(`group_id IS NULL`)는 `40개 이상`이어야 함.
- 공개 문서는 학생, 멘토, 관리자, 운영진이 작성했을 법한 문서로 구성되어야 함.
- 공개 문서 예시 주제:
  - 센터별 오리엔테이션
  - 예비 과정 안내
  - 본 과정 운영 일정
  - 멘토링 운영 기준
  - 프로젝트 활동비 사용 가이드
  - IT기기 지원 체크리스트
  - 데모데이 발표 가이드
  - IR Deck 작성 팁
  - 고객 인터뷰 가이드
  - MVP 범위 정의 방법
  - AI 윤리 / 개인정보 / 접근성 체크리스트
  - 창업팀 협업 규칙
- 공개 문서 하나당 해시태그는 `8개 내외`로 정교하게 달려 있어야 함.

6. 해시태그
- 해시태그 풀은 충분히 커야 함. 예: `창업`, `AI`, `LLM`, `MVP`, `PMF`, `SaaS`, `B2B`, `B2C`, `모빌리티`, `에듀테크`, `헬스케어`, `접근성`, `핀테크`, `생산성`, `백엔드`, `프론트엔드`, `데이터`, `추천시스템`, `RAG`, `온보딩`, `IR`, `고객인터뷰`, `UX`, `A/B테스트` 등.
- 공개 문서는 문서당 해시태그 `8개 내외`, 팀 문서는 문서당 `4~6개` 정도 구성이어야 함.
- `doc_hash_tag`는 실제 조회했을 때 문서마다 주제가 선명하게 느껴질 정도로 충분히 연결되어야 함.

7. 관리자 `소마멘토` 기준 시연 데이터
- `소마멘토`로 로그인했을 때 빈 목록이 보이지 않는 상태여야 함.
- 최소 기준:
  - `bookmarks` 5건 이상
  - `note` 5건 이상
  - `comment` 5건 이상
  - `request`와 연관된 데이터 5건 이상
  - `user_doc_authority` 5건 이상
  - `version.writer_id = 소마멘토`인 버전 5건 이상
- 현재 스키마상 승인된 문서 열람 권한은 `request` 승인 상태와 `user_doc_authority` 조합으로 표현되므로, 승인 완료 데이터는 두 테이블이 맞물려야 함.

8. 권한 요청 / 승인 / 알림
- 권한 요청은 `pending`, `approved`, `refused` 상태를 모두 포함해야 함.
- 요청 데이터는 학생 -> 멘토 검토 문서, 멘토 -> 타팀 열람 요청, 학생 -> 공용 참고문서 열람 요청 같은 현실적인 시나리오여야 함.
- 승인된 요청은 due_date가 자연스럽게 들어가야 하며, 대응하는 `user_doc_authority`도 함께 생성되어야 함.
- `note`는 승인/반려/멘토 피드백/운영 공지 맥락으로 충분히 생성되어야 함.

9. 즐겨찾기 / 댓글 / 첨부파일
- 공개 문서와 팀 문서 모두에 북마크가 분산되어야 함.
- 댓글은 학생, 멘토, 관리자가 남길 법한 짧은 피드백/질문/요청 형태로 자연스러워야 함.
- `comment.user_name`은 반드시 실제 작성자 `user.name`과 일치해야 함.
- `attached_file`은 실제 외부 파일 없이도 동작하도록 예시 URL을 사용해야 함.
  - 예: `https://vue-spring-s3.huposit.kr/demo/pitch-deck/team-001-v1.pdf`
- 발표자료, 와이어프레임, 사업계획서 PDF, 데이터셋 설명서 같은 첨부파일도 생성해야 함.

10. 데이터 품질
- 문서 제목, 팀명, 사용자명, 태그가 지나치게 반복적으로 보이지 않아야 함.
- 동일한 패턴의 문장을 복붙한 것처럼 보이지 않게 내용을 적절히 변주해야 함.
- 문서는 면접관이 봤을 때 "실제로 운영 중인 17기 프로그램 + 팀 프로젝트 협업 툴"처럼 느껴져야 함.
- 데이터 양이 많더라도, 주요 화면에서 눌러볼 대표 팀/대표 문서가 눈에 띄어야 함.

SQL 작성 원칙:
- 가능하면 `INSERT ... SELECT ... WHERE NOT EXISTS (...)`
- 또는 `INSERT IGNORE`
- 필요 시 `UPDATE ... WHERE ...`는 최소 범위로만 사용해야 함.
- 사용자/그룹/문서 매핑은 제목, employee_id, group_name 등으로 찾아서 연결해야 함.
- 기존 기본 시드와 충돌하면 안 됨.
- 실행 즉시 데모에서 조회 가능한 수준으로 관계가 채워져 있어야 함.
- 신규 그룹을 만들 때는 해당 그룹의 최상단 안내 문서(`SECTION`)와 버전도 함께 생성되어야 함.
- 공개 문서는 `group_id IS NULL`이어야 함.
- 그룹 문서는 같은 그룹 내부에서 문서 체인이 이어져야 함.
- `request.group_id`는 반드시 해당 문서의 소속 그룹과 일치해야 함.
- `bookmarks.documentId`, `comment.documentId` 컬럼명을 정확히 사용해야 함.
- `user_doc_authority.due_date`는 승인된 요청의 기간과 자연스럽게 연결되어야 함.

마지막 출력 형식:
- SQL only여야 함.
- 설명은 금지됨.
- 주석은 최소한으로만 허용됨.
```

## 인프라 적용 방식

생성된 `infra-handoff/seed-rich.sql`은 아래 방식으로 바로 주입 가능하다.

### 실행 중인 DB에 직접 주입

```bash
docker exec -i kip-demo-mariadb-1 mariadb -ukip -pkip1234 kip < infra-handoff/seed-rich.sql
```

### 최초 기동 시 자동 주입

```text
/docker-entrypoint-initdb.d/20-seed-rich.sql
```

단, 이 방식은 `MariaDB 데이터 디렉토리가 비어 있을 때만` 자동 실행된다.

## 주의할 점

- SQL은 DB 데이터만 넣는다.
- 현재 앱 구조상 대량 시드 SQL은 `애플리케이션 코드`가 아니라 `인프라 레이어`에서 관리하는 편이 낫다.
- 이미 기동한 DB에 다시 넣을 때는 반드시 `idempotent` 해야 함.
